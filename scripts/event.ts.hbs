import type Event from './eventTypes';

const {{name}}Event: Event<'{{name}}'> = {
  name: '{{name}}',
  // TODO: set this to false if event is not intended to be one-time
  once: true,
  async execute(args) {},
};

export default {{name}}Event;
