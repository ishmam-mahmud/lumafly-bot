{
  "name": "@lumafly/db",
  "version": "1.0.0",
  "description": "Db and ORM for Lumafly",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:start": "docker-compose up"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "prisma": "^3.3.0",
    "ts-node": "^10.4.0"
  },
  "dependencies": {
    "@prisma/client": "^3.3.0"
  }
}